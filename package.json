{
  "name": "ofds-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:gateway": "cd gateway && npm run dev",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:gateway\" \"npm run dev:frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../gateway && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.1"
  }
}
